# Contributing
Please keep all updates related to training the network within the ``training`` branch.  
Likewise all updates to the interface should be kept within the ``interface`` branch.

Please to not attempt to directly merge any branches directly into ``main``, instead use a pull-request.
